<%= f.hidden_field :current_step, value: f.object.current_step %>

<%= render "employees/steps/page#{f.object.current_step + 1}", f: f %>

<div class="field">
  <%= f.submit f.object.last_step? ? nil: "Next", :class => "btn btn-success submit-btn step_#{f.object.current_step}" %>
</div>
